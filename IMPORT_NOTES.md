# Import Branch for synapse_v100_rc_fixes

This branch was prepared to receive the extracted contents of `synapse_v100_rc_fixes`.

Planned import layout:
- strip the outer `synapse_project/` folder
- place `package.json`, `vite.config.js`, `index.html`, and `src/` at repo root
- keep the import isolated from `main` until reviewed

Status:
- branch created successfully
- local archive inspected and extracted
- clean root export prepared outside GitHub for upload/review
